-r requirements.txt

# --- Development & test dependencies ---
pytest>=8.3,<9.0
pytest-asyncio>=0.24,<1.0
httpx>=0.27,<1.0          # ASGI test client transport
ruff>=0.7,<1.0           # lint + format
